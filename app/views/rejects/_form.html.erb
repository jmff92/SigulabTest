# encoding: utf-8

<script>

	function HandleBrowseClick()
	{
		 var fileinput = document.getElementById("browse");
		 fileinput.click();
	}

	function Handlechange()
	{
		 var fileinput = document.getElementById("browse");
		 var textinput = document.getElementById("filename");
		 textinput.value = fileinput.value;
	}
        

</script>

<div style="margin-left:0px;">
	<%= form_for(@reject) do |f| %>
  		 <%= @reject.errors.full_messages.first if @reject.errors.any? %> 
<br/>
      	<br>	
  	
  <div class="field"><center>
          <span class="red">*</span>
			 <%= f.label "Seleccione una opción: " %><br><br>
			  <%= f.collection_select :estado, Reject::Aceptacion, :to_s, :to_s, 
          :include_blank => false %></center>
                  </div>  <br/>
        <div class="actions"><center>
 <% if session['specification_p6'] == 2 %>
    <%= f.submit "Aceptar", data: { confirm: '¿Está seguro de la informacion que acaba de suministrar?' }  %>
<% else %>
<%= f.label "No puede aceptar / rechazar la solicitud hasta haber generado una solicitud de servicio" %>
	<% end %>
<% end %>
     </center> </div>
</div>

<style>

input[type="submit"]  {

	display: inline-block;
	padding: 6px 12px;
	margin-bottom: 0;
	font-size: 14px;
	font-weight: 400;
	line-height: 1.42857143;
	text-align: center;
	white-space: nowrap;
	vertical-align: middle;
	color: #fff;
	background-color: #428bca;
	border-color: #357ebd;
	border: 1px solid transparent;
	border-radius: 4px;


}

#fakeBrowse {

position:relative;
left: 220px;
bottom: 34px;
}

.red {

	font-size: 20px;
	color: red;

}


input[type="text"] {

display: block;
width: 200px;
height: 34px;
padding: 6px 12px;
font-size: 14px;
line-height: 1.42857143;
color: #555;
background-color: #fff;
background-image: none;
border: 1px solid #ccc;
border-radius: 4px;
maxlength:100;

}

input[type="text"]:focus { 
    outline: none;
    border-color: #9ecaed;
    box-shadow: 0 0 10px #9ecaed;
}

h2 {

position:relative;
right: 150px;
color:red;
font-size: 20px;
right: 60px;
}

.field_with_errors {
  	background-color: red;
  	border-radius: 4px;
}
select {
    padding:3px;
    margin: 0;
    -webkit-border-radius:4px;
    -moz-border-radius:4px;
    border-radius:4px;
    background: white;
    outline:none;
    display: inline-block;
    cursor:pointer;
    width: 200px;
    height: 35px;
    maxlength:100;
    font-size: 14px;
}

</style>
<script>
$(document).ready(function() {
    $('#selecctall').click(function(event) {  //on click 
        if(this.checked) { // check select status
            $('.checkbox1').each(function() { //loop through each checkbox
                this.checked = true;  //select all checkboxes with class "checkbox1"               
            });
        }else{
            $('.checkbox1').each(function() { //loop through each checkbox
                this.checked = false; //deselect all checkboxes with class "checkbox1"                       
            });         
        }
    });
    
});
</script>
